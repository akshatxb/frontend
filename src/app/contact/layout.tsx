const ContactLayout = (
    { children }: { children: React.ReactNode }) => {

    return (
        <div className="bg-white text-black">
            {children}
        </div>
    )
}


export default ContactLayout